# 1. Write a script which accepts a sequence of comma-separated numbers from user and generate
#  a list and a tuple with those numbers.
nums = input("Enter some numbers: ")
nums_lst = list(nums.split(","))
nums_tuple = tuple(nums.split(","))
# print(nums_lst)
# print(nums_tuple)
